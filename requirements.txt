# ── Core AI ────────────────────────────────────────────────────────────────────
anthropic>=0.40.0

# ── YouTube extraction ─────────────────────────────────────────────────────────
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.0

# ── Image acquisition ──────────────────────────────────────────────────────────
requests>=2.31.0
Pillow>=10.0.0          # conversão de imagem para JPEG (opcional mas recomendado)

# ── TTS — edge-tts (Microsoft Azure Neural, gratuito, com word boundaries) ─────
edge-tts>=6.1.0

# ── Legendas word-level para vozes locais (forced alignment em CPU) ────────────
# Opcional: só é usado se a voz NÃO vier do edge-tts.
# stable-ts>=2.17
# faster-whisper>=1.0

# ── Audio ──────────────────────────────────────────────────────────────────────
numpy>=1.24.0
soundfile>=0.12.1

# ── Tradução multi-idioma (v3.8) ───────────────────────────────────────────────
deep-translator>=1.11

# ── Studio multi-agentes (v6) ──────────────────────────────────────────────────
ddgs>=9.0                # busca web gratuita (Agente Pesquisador)

# ── Motores TTS instalados (o benchmark detecta e usa o melhor disponível) ─────
qwen-tts                 # Qwen3-TTS 0.6B local (Apache 2.0) — PRINCIPAL
kokoro-onnx>=0.4         # Kokoro local rápido (arquivos kokoro-v1.0.onnx +
                         # voices-v1.0.bin na raiz do projeto)
# Opcionais:
# google-cloud-texttospeech   → Chirp 3 HD, 1M chars/mês grátis (exige credencial)
# chatterbox-tts              → voz local premium PT-BR (MIT, lenta em CPU)
# piper-tts                   → fallback offline ultrarrápido

# ── FFmpeg: binário externo, instalar separadamente ───────────────────────────
# Windows: https://www.gyan.dev/ffmpeg/builds/  (ffmpeg-release-essentials.zip)
# Descompactar e adicionar bin/ ao PATH do sistema
# (alternativa sem PATH: pip install imageio-ffmpeg)
